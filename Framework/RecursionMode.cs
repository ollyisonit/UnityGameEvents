namespace dninosores.UnityGameEvents
{
	/// <summary>
	/// Different ways that GameEvents can be searched for in children.
	/// </summary>
	public enum RecursionMode
	{
		OnlyIfEmpty = 0,
		TopLevelOnly = 1,
		Complete = 2
	}
}
